PREFIX pv: <https://rdfox-stress.example.org/prov/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

# Baseline: count every quad in every named graph.
# Pure scan speed.
SELECT (COUNT(*) AS ?quads)
WHERE {
  GRAPH ?g { ?s ?p ?o }
}
